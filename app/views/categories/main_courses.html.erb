<h1> Main Courses </h1>
  <ul>
      <% @recipes.each do |recipe| %>
      <% if recipe.category == 'Main' %>
        <li><%= link_to recipe.recipe_name, recipe_path(recipe.id) %></li>
      <% end %>
    <% end %>
  </ul>